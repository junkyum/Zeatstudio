import { useEffect } from "react";
import { delay, motion, spring, useAnimationControls } from "framer-motion";

const TextEffect = ({ texts }: { texts: string[] }) => {
  useEffect(() => {}, []);

  return (
    <>
      <motion.div></motion.div>
    </>
  );
};
export default TextEffect;
